{
  "name": "portfolio-react",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run db:up && concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "setup": "npm run install:all && npm run db:up && npm run db:seed",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "db:up": "bash scripts/db-up.sh",
    "db:down": "bash scripts/db-down.sh",
    "db:seed": "npm run seed --prefix server",
    "db:migrate": "npm run migrate --prefix server",
    "db:reset": "npm run reset --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
